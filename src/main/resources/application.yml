spring:
    datasource:
        url: jdbc:postgresql://localhost/db_springboot_backend_template?useSSL=false
        username: postgres
        password: deg159753
        driver-class-name: org.postgresql.Driver
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: create-drop
logging:
    level:
        org:
            hibernate:
                SQL: debug